# Write metadata to the video file
--add-metadata

# Specify audio format:
# 'best', 'aac', 'flac', 'mp3', 'm4a', 'opus', 'vorbis', or 'wav';
# 'best' by default
--audio-format 'm4a'

# Specify ffmpeg audio quality,
# insert a value between 0 (better) and 9 (worse)
--audio-quality 0

# Display progress in console titlebar
--console-title

# Embed thumbnail in the audio as cover art
#
# 20200823
# 🐛 That param may make the video > audio conversion to fail
# because sometimes the thumbnail is corrupted
#
# See related issue:
# [--embed-thumbnail conversion failed, ffmpeg and .webp images](https://github.com/ytdl-org/youtube-dl/issues/25687)
# —Maylis
# --embed-thumbnail

# Bypass geographic restriction via faking X-Forwarded-For HTTP header
--geo-bypass

# Continue on download errors, for example to skip unavailable videos in a playlist
-i

# Mark videos watched (YouTube only)
--mark-watched

# Save all downloads under `Dropbox/music` directory
-o '/Volumes/Storage/Dropbox/music/%(title)s.%(ext)s'

# Do not overwrite files
# -w

# Write video description to a .description file
#
# 20200823
# 🐛 Not working (description is truncated)
# See related issue:
# [Youtube description info truncates at 160 chars](https://github.com/ytdl-org/youtube-dl/issues/25937)
# —Maylis
--write-description

# Write thumbnail image to disk
--write-thumbnail

# Convert video files to audio-only files (requires ffmpeg)
-x

# Download the playlist, if the URL refers to a video and a playlist
--yes-playlist
